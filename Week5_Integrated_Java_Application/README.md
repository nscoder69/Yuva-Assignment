# Week 5 Integrated Library Inventory Application

Requirements: Java 17+.

Build on Linux/macOS:
sh deployment/build.sh

Run:
sh deployment/run.sh

Windows:
deployment\build.bat

The application integrates Book, LibraryManager, and Main into one command-line Java application. The deployment scripts compile the sources and package an executable JAR.
